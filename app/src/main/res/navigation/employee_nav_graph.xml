<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/employee_nav_graph"
    app:startDestination="@id/homescreen">

    <fragment android:name="com.project.employeeLogs.views.fragments.EmployeeLogsFragment"
        android:label="HomeScreenFragment"
        android:id="@+id/homescreen"
        tools:layout="@layout/fragment_employee_logs">
        <action android:id="@+id/homescreen_to_detailscreen"
            app:destination="@id/animeFragment"
            app:launchSingleTop="true"
            app:popUpToInclusive="true"
            app:popUpTo="@id/homescreen"/>
    </fragment>

    <fragment android:name="com.project.employeeLogs.views.fragments.AnimeFragment"
        android:label="AnimeFragment"
        android:id="@+id/animeFragment"
        tools:layout="@layout/fragment_anime">

    </fragment>

</navigation>